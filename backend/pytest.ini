[pytest]
# Put the backend root on sys.path so `import app` works whether tests are run
# as `pytest` (console script) or `python -m pytest`. Without this, the console
# script doesn't add the cwd and collection fails with import errors.
pythonpath = .
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
